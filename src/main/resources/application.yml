spring:
  application:
    name: "help-desk-backend"
  datasource:
    url: jdbc:mysql://localhost:3306/spring-ai-project?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Priyanshu@12
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  server:
    port: 8080
  ai:
    google:
      genai:
        api-key: ${GENAI_API_KEY}
        chat:
          options:
            model: gemini-2.5-flash
  logging:
    level:
      root: INFO
      org:
        springframework:
          ai: TRACE
